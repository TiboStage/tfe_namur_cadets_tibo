{#
    AnonymousComponent de compatibilité.
    Délègue au partial privé avec role='owner' par défaut.

    Usage direct recommandé :
      {{ include('workshop/_partials/_project_card_private.html.twig', { project: project, role: role }) }}
      {{ include('workshop/_partials/_project_card_public.html.twig',  { project: project }) }}
#}
{% set role = role|default('owner') %}
{{ include('workshop/_partials/_project_card_private.html.twig', { project: project, role: role }) }}
