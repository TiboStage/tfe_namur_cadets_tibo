twig:
    file_name_pattern: '*.twig'

    globals:
        app_version: '@App\Service\VersionService'

when@test:
    twig:
        strict_variables: true
        form_themes:
            - 'form/form_errors.html.twig'
